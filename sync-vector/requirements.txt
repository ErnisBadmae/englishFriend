# Core dependencies
kafka-python==2.0.2
qdrant-client==1.7.0
pyyaml==6.0.1

# Async support
asyncio-mqtt==0.16.1

# HTTP client for health checks
httpx==0.25.2  # Внимание: если устанавливать в одно окружение с основным проектом, нужен httpx>=0.27.1
aiohttp==3.9.1

# Metrics and observability
prometheus-client==0.19.0

# Development dependencies
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-mock==3.12.0
